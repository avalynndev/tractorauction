# ============================================
# .env FILE - WHAT IT SHOULD LOOK LIKE
# ============================================

# Database
# ⚠️ CHANGE 'root' TO YOUR ACTUAL POSTGRESQL PASSWORD ⚠️
DATABASE_URL="postgresql://postgres:root@localhost:5432/tractorauction?schema=public"

# JWT Secret
JWT_SECRET="your-secret-key-change-in-production"

# Cron Job Secret (for auction status automation)
# Generate with: openssl rand -hex 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CRON_SECRET="your-cron-secret-key-change-in-production"

# OTP Configuration
OTP_EXPIRY_MINUTES=10

# App URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Image Upload Service (Cloudinary)
# Get these from: https://console.cloudinary.com/
# Sign up at https://cloudinary.com/users/register/free
# 
# Option 1: Use individual variables (RECOMMENDED - what we're using)
CLOUDINARY_CLOUD_NAME="your_cloud_name"  # Your Cloudinary cloud name
CLOUDINARY_API_KEY="your_api_key"  # Your Cloudinary API key
CLOUDINARY_API_SECRET="your_api_secret"  # Your Cloudinary API secret (keep this secret!)
#
# Option 2: Use single CLOUDINARY_URL (ALTERNATIVE - not required if using Option 1)
# Format: cloudinary://api_key:api_secret@cloud_name
# CLOUDINARY_URL="cloudinary://your_api_key:your_api_secret@your_cloud_name"
# 
# NOTE: You only need ONE of the above options. We use Option 1 (individual variables).
# The "API Environment Variable" shown in Cloudinary dashboard is the CLOUDINARY_URL (Option 2).
# It's IGNORABLE if you're using the three individual variables above.

# SMS Service - Choose ONE provider (or use multiple for fallback)

# Option 1: Twilio (International)
# Get these from: https://console.twilio.com/
TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
TWILIO_AUTH_TOKEN="your_auth_token_here"
TWILIO_PHONE_NUMBER="+1234567890"

# Option 2: MSG91 (Recommended for India - Lower cost, better delivery)
# Get these from: https://msg91.com/
# Sign up, complete KYC, get Auth Key and Sender ID
MSG91_AUTH_KEY="your_msg91_auth_key_here"
MSG91_SENDER_ID="TRACTR"  # 6 characters max

# Option 3: TextLocal (Easy setup for India)
# Get these from: https://www.textlocal.in/
TEXTLOCAL_API_KEY="your_textlocal_api_key_here"
TEXTLOCAL_SENDER_ID="TRACTR"  # 6 characters max

# Payment Gateway (Razorpay)
# Get these from: https://dashboard.razorpay.com/app/keys
# Use Test Keys for development, Live Keys for production
RAZORPAY_KEY_ID="rzp_test_xxxxxxxxxxxxx"  # Your Razorpay Key ID
RAZORPAY_KEY_SECRET="xxxxxxxxxxxxxxxxxxxx"  # Your Razorpay Key Secret (keep this secret!)
NEXT_PUBLIC_BASE_URL="http://localhost:3000"  # Your app URL (for payment callbacks)

# Email Service - Choose ONE provider (SendGrid recommended)
# See EMAIL_PROVIDER_GUIDE.md for detailed setup instructions

# Option 1: SendGrid (RECOMMENDED - Easy setup, 100 free emails/day)
# Get API key from: https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY="SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
FROM_EMAIL="noreply@tractorauction.in"  # Use your verified email or domain

# Option 2: SMTP (Gmail/Outlook - For testing only)
# Gmail: Enable 2FA, then create App Password at https://myaccount.google.com/apppasswords
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_SECURE="false"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASS="your-16-character-app-password"
# FROM_EMAIL="your-email@gmail.com"

# Option 3: AWS SES (For high volume - requires AWS account)
# AWS_ACCESS_KEY_ID="AKIAxxxxxxxxxxxxxxxx"
# AWS_SECRET_ACCESS_KEY="xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# AWS_REGION="ap-south-1"  # Mumbai region for India
# FROM_EMAIL="noreply@tractorauction.in"

# ============================================
# INSTRUCTIONS:
# ============================================
# 1. Open .env file in your editor
# 2. Find the line: DATABASE_URL="postgresql://postgres:root@..."
# 3. Replace 'root' with your actual PostgreSQL password
# 4. Save the file
# 5. Run: npx prisma db push
# ============================================

# EXAMPLE:
# If your password is "mypassword123", change:
# FROM: DATABASE_URL="postgresql://postgres:root@localhost:5432/tractorauction?schema=public"
# TO:   DATABASE_URL="postgresql://postgres:mypassword123@localhost:5432/tractorauction?schema=public"

